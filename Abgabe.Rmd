---
title: "Testing and comparing different R packages on Random Forest"
author: "Riedlmeier, Moritz; Schröter, Timo; Steins, Patricia; Zinsmeister, Philipp"
date: "August 30, 2024"
output:
  html_notebook: 
    toc: true
    number_sections: true
    theme: simplex
  pdf_document: default
subtitle: RandomForest, ranger & caret
bibliography: references.bib
---

# Introduction: What is a random forest? Motivation/research question

# How is a random forest structured/theory/hypotheses

```{r setup, include=FALSE}

# load the basic packages
if (!require("pacman")) {
  install.packages("pacman")
} 
if (!require("hardhat")) {
  install.packages("hardhat")
}

# load the useful packages
pacman::p_load(tidyverse, hardhat, purrr, MLmetrics, foreach, doParallel, tibble,
               paletteer)

# load the packages with randomforest
pacman::p_load(randomForest, randomForestSRC, ranger,
               caret)

# generate function for simulating data

# generate function for crossvalidation

```

# Organisation of the study

In line with the research question, empirical and simulated multinomial data is used while drawing on different implementations of the Random Forests algorithm to classify this data. Since the focus lies on comparing different R packages, different goodness of fit measures are considered after classification to therefore evaluate the performance of the R packages.

# Preparing the data

For the analysis, on the one hand, empirical data is used. The data set originally stems from (...) and contains data on cover tree types. In forestry, a cover tree type is determined by the most prevalent tree type in a certain forest area (REFERENCE). Since the data set contains not only a large amount of observations but also many predictors, it is suitable for the use in the study. On the other hand, simulated data was used for which it is also aimed at arriving at a large number of observations and predictors. In the following, the simulation of the second data set is described which is used in the analysis. Randomly, it is drawn from different (WHICH?) distributions to get artificial multinomial data and predictor variables. The simulated multinomial data then consists of (HOW MANY) different tree types, on the one hand. On the other hand, a large number of predictors is obtained. Moreover, correlations between the predictors were included in the simulation process as these would be found in an empirical data set.(HOW DID WE INCLUDE THE CORRELATIONS)

# The different packages

1.  randomForest: Classification and regression based on a forest of trees using random inputs, based on [@breiman2001].
2.  ranger: A fast implementation of Random Forests, particularly suited for high dimensional data. Ensembles of classification, regression, survival and probability prediction trees are supported.
3.  caret: Misc. functions for training and plotting classification and regression models.

# Cross-validation

# Results

# Conclusion

# References
